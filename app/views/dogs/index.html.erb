<div class = "navbar">
<h1>Dogs For Days 🐶 </h1>
<%= link_to 'HOME', root_path, class: 'card-category-link'%>
</div>
  <div class="container">

  </div>

<div class="container my-5">
 <h1>Discover our cute dogs</h1>
  <div class="cards">
    <% @dogs.each do |dog| %>
      <div class= "dog-cards" style="background">
        <%= dog.name %>
         <%= image_tag dog.image_url %>
        <%= link_to 'PET ME', dog_path(dog), class: 'card-category-link'%>
      </div>
    <% end %>
  </div>
</div>

